class Application:
	
	def __init__(self, app_id, user, name, version, timezone, notifications, tier, packages):
		self.app_id = app_id
		self.user_id = user_id
		self.name = name
		self.version = version
		self.timezone = timezone
		self.notifications = notifications
		self.tier = tier
		self.packages = packages
