class Content:

	def __init__(self, content_id, application, user, name, collaborators, input_type):
		self.content_id = content_id
		self.application = application
		self.user = user
		self.name = name
		self.collaborators = collaborators
		self.input_type = input_type
